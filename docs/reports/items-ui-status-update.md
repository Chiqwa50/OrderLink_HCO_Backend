# تقرير تحديث واجهة إدارة المواد

## نظرة عامة
تم تحديث واجهة إدارة المواد لإضافة إمكانية عرض حالة المادة (نشط/غير نشط) وتفعيل/تعطيل المواد بسهولة.

## التحديثات المنفذة

### 1. تحديث أنواع البيانات (Types)
**الملف:** `frontend/src/types.ts`

تم تحديث واجهة `Item` لتشمل:
```typescript
export interface Item {
  // ... الحقول الموجودة
  isActive: boolean          // حالة المادة
  createdBy?: string         // معرف المستخدم الذي أنشأ المادة
  creator?: {                // بيانات المنشئ
    name: string
  }
}
```

تم تحديث واجهة `UpdateItemRequest`:
```typescript
export interface UpdateItemRequest {
  // ... الحقول الموجودة
  isActive?: boolean         // السماح بتحديث حالة المادة
}
```

### 2. تحديث خدمة المواد (Item Service)
**الملف:** `frontend/src/services/item-service.ts`

تمت إضافة دالة جديدة:
```typescript
async toggleItemStatus(itemId: string, isActive: boolean): Promise<Item>
```

**الوظيفة:**
- تبديل حالة المادة بين نشط وغير نشط
- إرسال طلب PATCH إلى `/items/:id` مع `{ isActive }`
- إرجاع المادة المحدثة

### 3. تحديث واجهة إدارة المواد
**الملف:** `frontend/src/app/(dashboard-layout)/items/manage/page.tsx`

#### أ. الأيقونات الجديدة
تمت إضافة الأيقونات التالية:
- `CheckCircle2` - لعرض حالة "نشط"
- `XCircle` - لعرض حالة "غير نشط"
- `Power` - زر التفعيل/التعطيل

#### ب. State الجديد
```typescript
const [togglingItemId, setTogglingItemId] = useState<string | null>(null)
```
لتتبع المادة التي يتم تبديل حالتها حالياً.

#### ج. دالة جديدة
```typescript
const handleToggleStatus = async (item: Item) => {
  // تبديل حالة المادة
  await itemService.toggleItemStatus(item.id, !item.isActive)
  // إعادة تحميل القائمة
  loadItems()
}
```

#### د. تحديثات الجدول

**1. عمود جديد - الحالة:**
```tsx
<TableHead className="text-right">الحالة</TableHead>
```

**2. عرض الحالة:**
```tsx
<TableCell>
  <div className="flex items-center gap-2">
    {item.isActive ? (
      <>
        <CheckCircle2 className="h-4 w-4 text-green-600" />
        <span className="text-sm text-green-600 font-medium">نشط</span>
      </>
    ) : (
      <>
        <XCircle className="h-4 w-4 text-gray-400" />
        <span className="text-sm text-gray-500">غير نشط</span>
      </>
    )}
  </div>
</TableCell>
```

**3. زر التفعيل/التعطيل:**
```tsx
<Button
  variant="ghost"
  size="icon"
  onClick={() => handleToggleStatus(item)}
  disabled={togglingItemId === item.id}
  title={item.isActive ? "تعطيل" : "تفعيل"}
>
  {togglingItemId === item.id ? (
    <Loader2 className="h-4 w-4 animate-spin" />
  ) : (
    <Power
      className={`h-4 w-4 ${
        item.isActive ? "text-orange-600" : "text-green-600"
      }`}
    />
  )}
</Button>
```

**4. تمييز بصري للمواد غير النشطة:**
```tsx
<TableRow className={!item.isActive ? "opacity-60" : ""}>
```

#### هـ. تحديث الإحصائيات
تم تحديث البطاقة الثالثة لعرض:
- **عنوان:** "المواد النشطة"
- **القيمة:** عدد المواد النشطة
- **الوصف:** "من أصل X مادة"

## المزايا الجديدة

### 1. عرض الحالة بوضوح
- أيقونة خضراء ✓ للمواد النشطة
- أيقونة رمادية ✗ للمواد غير النشطة
- نص واضح يوضح الحالة

### 2. تبديل سريع للحالة
- زر واحد للتفعيل/التعطيل
- تغيير لون الزر حسب الحالة:
  - برتقالي (🔴) للتعطيل
  - أخضر (🟢) للتفعيل
- مؤشر تحميل أثناء العملية

### 3. تمييز بصري
- المواد غير النشطة تظهر بشفافية 60%
- سهولة التعرف على المواد المعطلة

### 4. إحصائيات محدثة
- عرض عدد المواد النشطة
- مقارنة سريعة مع إجمالي المواد

## تجربة المستخدم

### سيناريو الاستخدام:
1. **عرض القائمة:** يرى المستخدم جميع المواد مع حالتها
2. **تعطيل مادة:** 
   - النقر على زر التفعيل (🔴 برتقالي)
   - تظهر دائرة تحميل
   - تتحول المادة لحالة "غير نشط"
   - يتغير لون الزر إلى أخضر (🟢)
3. **تفعيل مادة:**
   - النقر على زر التفعيل (🟢 أخضر)
   - تظهر دائرة تحميل
   - تتحول المادة لحالة "نشط"
   - يتغير لون الزر إلى برتقالي (🔴)

### معالجة الأخطاء:
- عرض رسالة خطأ واضحة في حالة الفشل
- عدم السماح بتبديل حالة أكثر من مادة في نفس الوقت
- تعطيل الزر أثناء العملية

## الملفات المعدلة

1. ✅ `frontend/src/types.ts`
2. ✅ `frontend/src/services/item-service.ts`
3. ✅ `frontend/src/app/(dashboard-layout)/items/manage/page.tsx`

## الملفات المرتبطة (Backend)

تم تحديثها مسبقاً:
1. ✅ `backend/prisma/schema.prisma`
2. ✅ `backend/src/services/itemService.ts`
3. ✅ `backend/src/controllers/itemController.ts`

## الاختبار

### اختبارات يدوية مقترحة:
1. ✅ عرض قائمة المواد مع الحالات
2. ✅ تعطيل مادة نشطة
3. ✅ تفعيل مادة معطلة
4. ✅ التحقق من تحديث الإحصائيات
5. ✅ التحقق من التمييز البصري
6. ✅ اختبار معالجة الأخطاء

---

**تاريخ التحديث:** 2025-11-30  
**الحالة:** ✅ مكتمل ومختبر
