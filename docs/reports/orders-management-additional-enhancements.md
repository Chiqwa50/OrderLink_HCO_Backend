# تحسينات إضافية لصفحة إدارة الطلبيات

**التاريخ**: 2025-12-02  
**الحالة**: ✅ مكتمل

## 📋 الملخص

تم إضافة تحسينات إضافية لصفحة إدارة الطلبيات تشمل:
1. عرض الوقت مع التاريخ
2. إضافة اسم المسؤول تحت اسم القسم
3. فلاتر متقدمة (المستودع، المستخدم)
4. دعم RTL كامل للفلاتر
5. مكون تاريخ عربي محسّن

## 🎯 التحديثات المنفذة

### 1. ✅ عرض التاريخ والوقت معاً

#### قبل:
```
التاريخ: 02/12/2025
```

#### بعد:
```
التاريخ: 02/12/2025 13:45
```

**الملفات المعدلة:**
- `orders-table.tsx`: تغيير من `formatDate` إلى `formatDateTime`

### 2. ✅ إضافة اسم المسؤول تحت اسم القسم

#### في الجدول:
```
┌──────────────┐
│ قسم الطوارئ  │
│ أحمد محمد    │ ← اسم المسؤول
└──────────────┘
```

#### في البطاقة:
```
👤 قسم الطوارئ
   أحمد محمد
```

**الملفات المعدلة:**
- `orders-table.tsx`: إضافة `createdByName` تحت `departmentName`
- `order-card.tsx`: إضافة `createdByName` مع أيقونة المستخدم

### 3. ✅ فلاتر متقدمة

#### الفلاتر الجديدة:

**أ. فلتر المستودع:**
```tsx
- جميع المستودعات
- مستودع الهنقر
- مستودع الأدوية
- مستودع المعدات
```

**ب. فلتر المستخدم:**
```tsx
- جميع المستخدمين
- أحمد محمد
- فاطمة علي
- محمد حسن
```

**الملفات المعدلة:**
- `order-filters.tsx`: إضافة فلاتر `warehouseId` و `createdBy`
- `manage/page.tsx`: إضافة منطق الفلترة واستخراج القوائم الفريدة

### 4. ✅ دعم RTL كامل

#### التحسينات:
```tsx
// جميع المكونات الآن تدعم RTL
<div dir="rtl">
  <Select dir="rtl">
    <SelectTrigger dir="rtl">
      <SelectContent dir="rtl">
  </Select>
</div>
```

**الخصائص المضافة:**
- `dir="rtl"` على جميع المكونات
- `className="text-right"` على حقول الإدخال
- `lang="ar-SA"` على حقول التاريخ

### 5. ✅ مكون التاريخ العربي

#### المميزات:
```tsx
<ArabicDateInput
  label="من تاريخ"
  value={dateFrom}
  onChange={handleChange}
  placeholder="اختر تاريخ البداية"
/>
```

**الوظائف:**
- عرض التاريخ بالأشهر العربية
- أيقونة تقويم
- دعم RTL كامل
- placeholder مخصص

**مثال العرض:**
```
📅 من تاريخ
   [2025-12-02]  ← حقل الإدخال
   2 ديسمبر 2025 ← عرض عربي
```

## 🏗️ البنية المعمارية

### المكونات الجديدة

#### 1. **ArabicDateInput** (`arabic-date-input.tsx`)
```typescript
interface ArabicDateInputProps {
  label: string
  value: string
  onChange: (value: string) => void
  placeholder?: string
  className?: string
}
```

**الوظائف:**
- `formatDateToArabic()`: تحويل التاريخ إلى صيغة عربية
- دعم RTL
- أيقونة تقويم
- عرض التاريخ بالأشهر العربية

### التحديثات على المكونات الموجودة

#### 1. **OrderFiltersComponent** (محدّث)
```typescript
interface OrderFilters {
  status?: OrderStatus | "ALL"
  departmentId?: string
  warehouseId?: string      // ← جديد
  createdBy?: string        // ← جديد
  searchTerm?: string
  dateFrom?: string
  dateTo?: string
}
```

**Props الجديدة:**
- `warehouses`: قائمة المستودعات
- `users`: قائمة المستخدمين
- `showWarehouseFilter`: إظهار/إخفاء فلتر المستودع
- `showUserFilter`: إظهار/إخفاء فلتر المستخدم

#### 2. **ManageOrdersPage** (محدّث)
```typescript
// استخراج القوائم الفريدة
const uniqueDepartments = Array.from(
  new Map(orders.map((o) => [
    o.departmentId, 
    { id: o.departmentId, name: o.departmentName }
  ])).values()
)

const uniqueWarehouses = Array.from(
  new Map(orders.map((o) => [
    o.warehouseId, 
    { id: o.warehouseId, name: o.warehouseName }
  ])).values()
)

const uniqueUsers = Array.from(
  new Map(orders.map((o) => [
    o.createdBy, 
    { id: o.createdBy, name: o.createdByName }
  ])).values()
)
```

## 📊 منطق الفلترة

### الفلترة المتقدمة:
```typescript
const applyFilters = () => {
  let result = [...orders]

  // 1. فلترة حسب الحالة
  if (filters.status && filters.status !== "ALL") {
    result = result.filter((o) => o.status === filters.status)
  }

  // 2. فلترة حسب القسم
  if (filters.departmentId && filters.departmentId !== "ALL") {
    result = result.filter((o) => o.departmentId === filters.departmentId)
  }

  // 3. فلترة حسب المستودع ← جديد
  if (filters.warehouseId && filters.warehouseId !== "ALL") {
    result = result.filter((o) => o.warehouseId === filters.warehouseId)
  }

  // 4. فلترة حسب المستخدم ← جديد
  if (filters.createdBy && filters.createdBy !== "ALL") {
    result = result.filter((o) => o.createdBy === filters.createdBy)
  }

  // 5. فلترة حسب التاريخ
  if (filters.dateFrom) {
    result = result.filter(
      (o) => new Date(o.createdAt) >= new Date(filters.dateFrom!)
    )
  }

  if (filters.dateTo) {
    result = result.filter(
      (o) => new Date(o.createdAt) <= new Date(filters.dateTo!)
    )
  }

  // 6. البحث النصي
  if (searchTerm) {
    result = result.filter(
      (o) =>
        o.orderNumber.toLowerCase().includes(searchTerm.toLowerCase()) ||
        o.departmentName.toLowerCase().includes(searchTerm.toLowerCase())
    )
  }

  setFilteredOrders(result)
}
```

## 🎨 واجهة المستخدم

### الجدول المحسّن:
```
┌────────────┬──────────────┬─────────────────┬────────┬──────────────────┬────────┐
│ رقم الطلبية│ القسم        │ المستودع        │ المواد │ التاريخ والوقت   │ الحالة │
├────────────┼──────────────┼─────────────────┼────────┼──────────────────┼────────┤
│ ORD-001    │ قسم الطوارئ  │ مستودع الهنقر   │   5    │ 02/12/2025 13:45 │ جديد   │
│            │ أحمد محمد    │ WH-001          │        │                  │        │
└────────────┴──────────────┴─────────────────┴────────┴──────────────────┴────────┘
```

### الفلاتر المحسّنة:
```
┌─────────────────────────────┐
│ 🔽 فلترة (3)               │
└─────────────────────────────┘
  ↓
┌─────────────────────────────┐
│ خيارات الفلترة    [مسح الكل]│
├─────────────────────────────┤
│ الحالة: [جميع الحالات ▼]   │
│ القسم: [جميع الأقسام ▼]    │
│ المستودع: [الكل ▼]         │ ← جديد
│ المستخدم: [الكل ▼]         │ ← جديد
│ 📅 من تاريخ: [__/__/____]  │ ← محسّن
│    2 ديسمبر 2025           │
│ 📅 إلى تاريخ: [__/__/____] │ ← محسّن
│    31 ديسمبر 2025          │
└─────────────────────────────┘
```

## 📁 الملفات المعدلة

### Frontend
1. ✅ `components/orders/orders-table.tsx` - عرض الوقت واسم المسؤول
2. ✅ `components/orders/order-card.tsx` - إضافة اسم المسؤول
3. ✅ `components/orders/order-filters.tsx` - فلاتر متقدمة + RTL
4. ✅ `components/ui/arabic-date-input.tsx` - مكون جديد
5. ✅ `app/(dashboard-layout)/orders/manage/page.tsx` - منطق الفلترة

## 🔍 أمثلة الاستخدام

### 1. فلترة حسب المستودع:
```
المستخدم يختار: "مستودع الهنقر"
النتيجة: عرض الطلبات المتجهة لمستودع الهنقر فقط
```

### 2. فلترة حسب المستخدم:
```
المستخدم يختار: "أحمد محمد"
النتيجة: عرض الطلبات التي أنشأها أحمد محمد فقط
```

### 3. فلترة مركبة:
```
الحالة: "قيد المراجعة"
المستودع: "مستودع الأدوية"
من تاريخ: "01/12/2025"
إلى تاريخ: "31/12/2025"

النتيجة: عرض الطلبات الجديدة المتجهة لمستودع الأدوية في ديسمبر 2025
```

## ✅ الخلاصة

تم تحسين صفحة إدارة الطلبيات بإضافة:
- ✅ عرض الوقت مع التاريخ
- ✅ اسم المسؤول تحت اسم القسم
- ✅ فلاتر المستودع والمستخدم
- ✅ دعم RTL كامل
- ✅ مكون تاريخ عربي محسّن
- ✅ تجربة مستخدم أفضل

جميع التحديثات تتبع مبادئ OOP وتوفر كود نظيف وقابل للصيانة.
